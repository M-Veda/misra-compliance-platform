# MISRA AI Compliance Agent — Project Cleanup Audit Report

> **Policy Notice**: No files are automatically deleted. Every item is classified with explicit rationale and safety checks.

---

## 1. Classification Summary

| Category | Item Count | Description |
| :--- | :---: | :--- |
| **Must Keep** | 32 Files | Core application source code (FastAPI backend, React frontend, 10 rule detectors, patch engine, state management, test suites). |
| **Review Before Delete** | 4 Files | Benchmark scripts, legacy wrappers, and debug helper tools that may be useful for developer benchmarking. |
| **Safe to Delete** | 5 Files | Temporary execution logs, raw test outputs, and scratch script files created during validation. |

---

## 2. Itemized Classification List

### 2.1 Must Keep (Essential Production Code)
1. `backend/api/main.py` — Core API entry point.
2. `backend/models/violation.py` — Core schemas and `generate_stable_id()`.
3. `backend/services/parser.py` — C preprocessor and `pycparser` AST service.
4. `backend/services/patch_engine.py` — Bottom-up transactional patch engine.
5. `backend/services/llm.py` — AI rule explanation service.
6. `backend/report/generator.py` — ReportLab PDF and JSON report generator.
7. `backend/rules/base.py` & `rule_*.py` (10 files) — All 10 MISRA rule AST detectors.
8. `frontend/src/context/AppContext.tsx` — Single-source-of-truth React Context.
9. `frontend/src/types/index.ts` — TypeScript types and `violationStableKey()`.
10. `frontend/src/components/*.tsx` (7 files) — All React UI tab components.
11. `backend/tests/test_rules.py` — Unit test suite for 10 rules.
12. `backend/tests/test_patch_engine.py` — Unit test suite for patch engine.
13. `backend/tests/test_e2e_stress.py` — E2E stress test suite.
14. `perf_test/*.c` (5 files) — Validation benchmark C source files.
15. `requirements.txt`, `README.md`, `package.json`, `tsconfig.json`, `vite.config.ts`.

---

### 2.2 Review Before Delete (Developer Utilities)
1. `api_integration_test.py` (Root): Standalone API test script. *Review*: Keep if offline API testing is needed.
2. `benchmark_performance.py` (Root): Benchmark runner script for timing uploads. *Review*: Keep for performance benchmarking.
3. `generate_example_reports.py` (Root): Standalone PDF report generator script. *Review*: Keep for testing PDF styling offline.
4. `backend/services/patch.py`: Legacy single-patch wrapper class. *Review*: Keep if third-party modules reference `PatchService`.

---

### 2.3 Safe to Delete (Temporary & Validation Artifacts)
1. `validation_output.txt` / `validation_output.log` (Root): Raw stdout logs from E2E test runs. *Safe to delete*.
2. `debug_17_7.py` / `debug_all_rules.py` (Root): Scratch debug scripts created during rule inspection. *Safe to delete*.
3. `evidence_data.json` (Root): Generated JSON metrics cache for the evidence report. *Safe to delete* (can be regenerated by `generate_comprehensive_evidence.py`).
4. `temp_preprocess.c` (Root): Transient file created by preprocessor during AST parsing. *Safe to delete*.
5. `backend_server.log` / `backend_server_err.log` (Root): Redirect logs from background server launches. *Safe to delete*.
